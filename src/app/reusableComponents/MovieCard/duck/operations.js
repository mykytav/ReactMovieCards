import Creators from './actions';

const addFavoriteMovie = Creators.addFavoriteMovie;
const removeFavoriteMovie = Creators.removeFavoriteMovie;
const fetchFavoriteMovies = Creators.fetchFavoriteMovies;

export default {
  addFavoriteMovie,
  removeFavoriteMovie,
  fetchFavoriteMovies
};

// import types from './types';

// const favoriteReducer = (state, action) => {
//   switch (action.type) {
//     case types.FETCH_FAVORITE_MOVIES:
//       return {
//         ...state,
//         movieCardReducer: { favoriteMovies: [...action.payload] }
//       };
//     default:
//       return state;
//   }
// };

// export default favoriteReducer;
